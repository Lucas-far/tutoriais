

def website():
    """"""

    # Ao entrar no site, crie uma conta (não há complexidade)
    # Criar a conta, é mandatória para poder criar o deploy, e gerenciá-lo
    # Porém, para que o Heroku possa ser usado, seu cliente precisa ser instalado (executável) (próximo passo)
    """ Pesquisa google: [ Heroku: Cloud Application Platform ] """
def instalar():
    """"""

    # Diferenças entre Linux & Windows
    # No Windows, baixa-se o instalador e instala-se normalmente
    # No Ubuntu, usa-se o comando [ sudo snap install --classic heroku ]
    """
    Link direto      # https://devcenter.heroku.com/articles/heroku-cli
    Pesquisa google  # The Heroku CLI | Heroku Dev Center
    """
